country_decisions = {
	papalcor = {
	major = yes
	potential = {
		tag = PAP
		religion = catholic
		is_religion_enabled = protestant
		random_owned_province = {
			limit = {
					development = 10
					NOT = { religion = catholic }
					}
		}
		NOT = { has_country_flag = catholic_cor }
	}
	allow = {
		tag = PAP
		religion = catholic
		is_religion_enabled = protestant
		NOT = { has_country_flag = catholic_cor }
		random_owned_province = {
			limit = {
					development = 10
					NOT = { religion = catholic }
					}
		}
	}
	effect = {
		set_country_flag = catholic_cor
		random_owned_province = {
			limit = {
					development = 10
					NOT = { religion = catholic }
					}
			change_religion = catholic
			add_reform_center = catholic
			add_base_tax = 1
		}
	}
	ai_will_do = {
		factor = 100
			}
		}
	}
}